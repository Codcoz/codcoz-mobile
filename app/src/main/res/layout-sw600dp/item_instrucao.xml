<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="32dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tvNumeroInstrucao"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_marginEnd="24dp"
        android:background="@drawable/bg_circle_number"
        android:fontFamily="@font/nunito_bold"
        android:minWidth="44dp"
        android:minHeight="44dp"
        android:text="1"
        android:textColor="@color/colorOnPrimary"
        android:textSize="22sp" />

    <TextView
        android:id="@+id/tvTextoInstrucao"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="@font/nunito_semibold"
        android:text="Pique a cebola, refogue por alguns minutos em uma panela com óleo quente até dourar a cebola, mexendo para não queimar."
        android:textColor="@color/colorOnSurfaceVariant"
        android:textSize="20sp" />

</LinearLayout>
